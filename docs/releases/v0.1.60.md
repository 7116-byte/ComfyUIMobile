# v0.1.60

- 修复图片全屏查看显示状态栏后，底部保存、分享等按钮被挤出原位置的问题。查看器始终保持沉浸式，图片控制栏显示与隐藏不改变内容布局。
- 修复结果页任务相册中的系统返回键直接退出 App：从全部平铺打开图片会返回全部平铺；从具体相册打开图片先返回该相册，再返回任务相册首页。
- 更新改为 App 内部下载并显示进度；下载完校验 SHA-256、APK 包名、版本号和签名，再调用 Android 系统安装确认页。Android 安装确认不能由普通 App 绕过。
- 更新文件位于 App 私有缓存，不再加入小米下载管理列表；一次只保留当前安装包。升级后会清理旧版本留在 App 外部下载目录中的 `ComfyUIMobile-v*-release.apk`、分段及校验文件，其他文件不会删除。
- 没有修改 VPN、代理或反向隧道配置。
