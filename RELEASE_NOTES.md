# v0.1.2

- 更新功能移入右上角设置，未连接时也可以打开设置检查更新。
- 连接过程改为六个顺序阶段，显示当前步骤、进度和每一步状态。
- 必须等待 ComfyUI 网页真正加载完成后才初始化前端，修复在空白页提前执行脚本的问题。
- 捕获网页加载错误、网页返回错误和加载超时，并在对应连接步骤中显示原因。

安全提醒：本应用只面向可信局域网中的无认证 ComfyUI，不要把 8188 端口暴露到公网。
